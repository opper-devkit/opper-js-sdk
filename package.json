{
  "name": "opper",
  "private": true,
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "build:docs": "typedoc",
    "lint": "eslint . --fix",
    "publish": "lerna publish --force-publish=* --exact",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.6.7",
    "@commitlint/config-conventional": "^17.6.7",
    "@sunkit/eslint-config": "^2.0.0",
    "@typescript-eslint/eslint-plugin": "^6.2.1",
    "@typescript-eslint/parser": "^6.2.1",
    "eslint": "^8.46.0",
    "husky": "^8.0.3",
    "lerna": "^7.0.0",
    "lint-staged": "^13.2.3",
    "tsup": "^7.1.0",
    "typedoc": "^0.24.8",
    "typescript": "^5.1.6"
  }
}
