{
  "name": "opper",
  "private": true,
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "build:docs": "typedoc",
    "lint": "eslint . --fix",
    "publish": "lerna publish --force-publish=* --exact",
    "publish:dev": "lerna run publish:dev",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@sunkit/eslint-config": "^2.0.0",
    "@typescript-eslint/eslint-plugin": "^6.2.1",
    "@typescript-eslint/parser": "^6.2.1",
    "eslint": "^8.46.0",
    "husky": "^8.0.3",
    "lerna": "^8.0.0",
    "lint-staged": "^15.0.0",
    "tsup": "^7.1.0",
    "typedoc": "^0.27.0",
    "typescript": "^5.1.6"
  }
}
